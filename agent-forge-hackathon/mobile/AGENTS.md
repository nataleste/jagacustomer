# Mobile Web App

This folder is a Vite + React mobile web app for the JAGA hackathon tracer.

- Prefer browser/mobile-web patterns over native or Expo code.
- Keep the app usable without Supabase credentials by preserving the local simulator path.
- Do not add `.env*` files. Document required `VITE_` variables in prose instead.
